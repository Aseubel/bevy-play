version: '3.8'

services:
  web:
    image: nginx:alpine
    container_name: bevy-play-web
    ports:
      - "8081:8081"
    volumes:
      - ./web:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: always
